<%= form_for(@group) do |f| %>
  
  <div class="field">
    <%= f.label "Group Name: " %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
